
# 🔧 Basic Git Commands

## Git Setup
```bash
git config --global user.name "Kusum Bhambhu"
git config --global user.email "your-bhambhukusum195@gmail.com"
git init
git clone <repo-url>
git status
git add .
git commit -m "Your commit message"
git push origin main
git pull origin main
git branch feature-branch
git checkout feature-branch
git merge feature-branch
git log
git remote -v
git rm --cached <file>
